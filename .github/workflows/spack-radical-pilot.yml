name: spack-radical-pilot

on:
  # https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#workflow_dispatch
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build strategy'
        type: choice
        options:
          - reuse
          - fresh
          - skip
        default: reuse
  # https://docs.github.com/en/rest/repos/repos#create-a-repository-dispatch-event
  repository_dispatch:
    types: [ ci-spack-rp-on-demand ]
  push:
    branches: [ main ]
    paths:
      - 'ci/envs/radical-pilot/**'
      - 'ci/tests/radical-pilot/**'

jobs:
  run:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/spack-base.yml
    with:
      tool_name: radical-pilot
      build_type: ${{ github.event_name == 'push' && 'fresh' || inputs.build_type || 'reuse' }}
    secrets: inherit
